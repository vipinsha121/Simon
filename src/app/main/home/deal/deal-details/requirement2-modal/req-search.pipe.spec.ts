import { Requirement2SearchPipe } from './req-search.pipe';

describe('Requirement2SearchPipe', () => {
  it('create an instance', () => {
    const pipe = new Requirement2SearchPipe();
    expect(pipe).toBeTruthy();
  });
});
