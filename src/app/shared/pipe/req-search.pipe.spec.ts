import { ReqSearchPipe } from './req-search.pipe';

describe('ReqSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ReqSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
